name: 'CI'

on:
  push:
    branches: [ main ]
  pull_request:
  workflow_dispatch:

jobs:
  lint:
    uses: ./.github/workflows/lint.yml

  build:
    uses: ./.github/workflows/build.yml
    needs: lint

  test:
    uses: ./.github/workflows/test.yml
    needs: build
